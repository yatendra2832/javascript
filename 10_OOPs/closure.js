/*function init(){
    let name='yatendra singh'
    function displayName(){
        console.log(name);
    }
    displayName()
}
// init()
*/
/*
function outer() {
    let username = 'hitesh'
    console.log(username);// that will print the name of the user when that function will be called
    function inner(){
        console.log('Inner :',username);
    }

}
// console.log(username);// that will print the exception as the username is not accessible outside the function

outer()
*/
// parent function varibales are accessible by the children but children valribales are not accessible by the parent function
//   when we return a function then not only the function is returning but also the whole lexical scope of the function will return 
